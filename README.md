# git-practice
Commands Practiced
<ul>
git init
git status
git add
git commit -m
git log --oneline
git checkout -b <branch>
git push -u origin <branch>
git pull
git branch -d <branch>
git remote add origin <url>
git remote -v
</ul>
